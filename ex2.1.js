'use strict';
 // Looping a triangle
for (var i = "#"; i.length <= 7; i += "#") {
    console.log (i);
  }

for (var j = "*"; j.length <= 4; j += "*") {
      console.log (j);
    }

console.log ( "Sruthi" .charAt (5));


var array = [1 , 2 , 3];
for ( var i = 0; i < array . length ; i ++) {
  var current = array [ i ];
  console . log ( current ) ;
}
